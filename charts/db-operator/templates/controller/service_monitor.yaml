{{- if and ( .Capabilities.APIVersions.Has "monitoring.coreos.com/v1" ) .Values.controller.serviceMonitor.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "db-operator.controller.name" $ }}
  labels:
    {{- include "db-operator.controller.labels" $ | nindent 4 }}
  {{- with .Values.controller.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.controller.service.type }}
  ports:
    - port: {{ .Values.controller.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
      {{- if and (eq .Values.controller.service.type "NodePort") (.Values.controller.service.nodePort) }}
      nodePort: {{ .Values.controller.service.nodePort }}
      {{- end }}
  selector:
    {{- include "db-operator.controller.selectorLabels" . | nindent 4 }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "db-operator.controller.name" $ }}
  labels:
    {{- include "db-operator.controller.labels" $ | nindent 4 }}
spec:
  {{- if .Values.controller.serviceMonitor.jobLabel }}
  jobLabel: {{ .Values.controller.serviceMonitor.jobLabel }}
  {{- end }}
  endpoints:
    - port: metrics
      {{- with .Values.controller.serviceMonitor.interval }}
      interval: {{ . }}
      {{- end }}
      {{- with .Values.controller.serviceMonitor.scrapeTimeout }}
      scrapeTimeout: {{ . }}
      {{- end }}
      {{- with .Values.controller.serviceMonitor.metricRelabelings }}
      metricRelabelings:
      {{- if kindIs "string" . }}
        {{- tpl . $ | nindent 8 }}
      {{- else }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      {{- with .Values.controller.serviceMonitor.relabelings }}
      relabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "db-operator.controller.selectorLabels" . | nindent 6 }}
{{- end }}
