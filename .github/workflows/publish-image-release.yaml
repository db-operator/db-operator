name: Publish a release image

on:
  release:
    types: published

jobs:
  build:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-qemu-action@v4
      - uses: go-task/setup-task@v2
      - run: task set-image-metadata
      - name: Set the operator version from the tag
        run: |-
          echo OPERATOR_VERSION=${{ github.event.release.tag_name }} >> "$GITHUB_ENV"
      - name: Build an image
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: db-operator
          tags: latest ${{ github.event.release.tag_name }}
          platforms: linux/amd64, linux/arm64/v8
          build-args: |
            OPERATOR_VERSION=${{ env.OPERATOR_VERSION }}
          containerfiles: |
            ./Containerfile
          labels: ${{ env.ANNOTATIONS }}
      - name: Push the image to GHCR
        id: push-to-ghcr
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image.outputs.image }}
          tags: ${{ steps.build-image.outputs.tags }}
          registry: ghcr.io/${{ github.repository_owner }}
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - uses: sigstore/cosign-installer@v4.1.1
      - uses: docker/login-action@v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: "${{ steps.push-to-ghcr.outputs.registry-path }}"
          scan-type: image
          format: "cosign-vuln"
          exit-code: "0"
          ignore-unfixed: false
          vuln-type: "os,library"
          severity: "CRITICAL,HIGH"
          output: ./report.json
      - name: Sign the image with GitHub OIDC Token
        env:
          DIGEST: ${{ steps.push-to-ghcr.outputs.digest }}
          IMAGE: ${{ steps.push-to-ghcr.outputs.registry-path }}
        run: |
          cosign sign --yes ${IMAGE}@${DIGEST}
          cosign attest --type vuln --predicate report.json ${IMAGE}@${DIGEST}
