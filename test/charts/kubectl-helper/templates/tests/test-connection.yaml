apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubectl-helper.fullname" . }}-test-connection"
  labels:
    {{- include "kubectl-helper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubectl-helper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
